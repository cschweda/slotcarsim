# No environment variables are required yet — this project is a 100%
# static build with no server-side config. This file is a placeholder for
# future configuration (e.g. a multiplayer/leaderboard backend); copy to
# .env and fill in values once any are introduced.
